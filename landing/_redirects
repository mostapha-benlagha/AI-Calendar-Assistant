# Netlify redirects file
# This file should be placed in the publish directory (landing/)

# Redirect old landing page URL to new index.html
/ai-calendar-landing.html /index.html 301

# Force HTTPS
http://* https://:splat 301!

# SPA fallback (if needed for future routing)
/* /index.html 200

# Redirect www to non-www (optional)
https://www.calendar-ai-assistant.netlify.app/* https://calendar-ai-assistant.netlify.app/:splat 301!

# Redirect trailing slash to non-trailing slash
/*/ /:splat 301
